package com.aq.game.individu.comportement.deplacement;

import com.aq.Frame;

public class DeplacementSage implements ComportementDeplacement {

    public void seDeplacer() {
        /*double a = Frame.joueur.getPosX() - this.posX;
        double b = Frame.joueur.getPosY() - this.posY;
        double l = Math.sqrt(a * a + b * b);
        a = a / l;
        b = b / l;
        if (l > 200)
            message = false;
        if (l > 150) {
            this.collisionMur();
            this.posX += a * 1.5;
            this.posY += b * 1.5;
        } else {
            message = true;
            this.collisionMur();
            this.posX -= a * 1.5;
            this.posY -= b * 1.5;
        }*/
    }
}