package com.aq.game.individu.comportement.deplacement;

public class DeplacementBoss implements ComportementDeplacement{
    
    public void seDeplacer() {
        /*attaque();
        double a = Frame.j.posX - this.posX;
        double b = Frame.j.posY - this.posY;
        double l = Math.sqrt(a * a + b * b);
        a = a / l;
        b = b / l;
        if (l > 90) {
            this.collisionMur();
            this.posX += a * 2;
            this.posY += b * 2;
        } else {
            this.collisionMur();
            this.posX -= a * 2;
            this.posY -= b * 2;
        }*/
    }
}